{
  "name": "hpc",
  "version": "1.0.0",
  "private": true,
  "description": "High Performers Club static site + VÉRO storefront (vero-store/)",
  "scripts": {
    "dev:vero": "npm --prefix vero-store run dev",
    "build:vero": "npm --prefix vero-store run build",
    "preview:vero": "npm --prefix vero-store run preview"
  }
}
